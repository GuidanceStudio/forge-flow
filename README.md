# devplan

Unified skill for **Claude Code** and **Codex** that handles the full devplan
lifecycle: planning (`design`) and execution (`TDD` / `IDD`).

> Placeholder README — full content lands in M8.
