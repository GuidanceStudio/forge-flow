interface:
  display_name: "Devplan"
  short_description: "Design or execute a devplan (design / TDD / IDD)"
  default_prompt: "Use $devplan to manage the current devplan — design new milestones, or execute them milestone by milestone with tests, docs, and commits."
